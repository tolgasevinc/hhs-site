{
  "name": "hhs-yeni-site-api-template",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "echo \"Bu template klasörü deploy edilmemeli. API için üst klasörde npm run deploy:api kullanın.\" && exit 1",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.4",
    "@types/node": "^25.6.0",
    "typescript": "^5.5.2",
    "vitest": "~3.2.0",
    "wrangler": "^4.87.0"
  }
}